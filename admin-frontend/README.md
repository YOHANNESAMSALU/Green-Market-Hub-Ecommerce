# Admin Frontend

Standalone admin dashboard app for MarketHub.

## Setup

1. Copy `.env.example` to `.env` and set `VITE_API_URL` to your shared backend.
2. Install dependencies: `npm install`
3. Run dev server: `npm run dev`
4. Build: `npm run build`

This app uses the same backend and database as the main marketplace project.
